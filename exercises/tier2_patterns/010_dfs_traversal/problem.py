from collections import defaultdict

def dfs(graph: dict[str, list[str]], start: str) -> list[str]:
    """Return nodes visited in DFS pre-order starting from `start`."""
    raise NotImplementedError("fill me in")
