[pytest]
# No testpaths — force users to pass an explicit exercise dir (or use marathon.py).
# Running all exercises at once would collide because every test imports `problem`.
python_files = test_problem.py
python_functions = test_*
addopts = -q --tb=short --no-header
