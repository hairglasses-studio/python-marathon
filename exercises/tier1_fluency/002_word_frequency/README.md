# Word Frequency Counter

**Tier:** tier1_fluency  
**Target time:** 10 minutes  
**Topics:** dict, string, counting  
**Source:** `openai-python-refresher.ipynb` cells 29-32

## Problem

## 2.5 Exercise — Word frequency counter

**Task:** Implement `word_freq(text)` returning a dict mapping each word to its count. Words are separated by whitespace. Case-insensitive (lowercase the words). Ignore empty strings.

**Follow-up:** return a list of `(word, count)` tuples sorted by count descending, then word ascending for ties.

## How to run

```bash
python marathon.py run 002
```

Edit `problem.py`. When `test_problem.py` passes, move on.
