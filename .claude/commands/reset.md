---
description: Reset an exercise's problem.py to the original stub
argument-hint: "NNN"
---

Run `python marathon.py reset $ARGUMENTS` from `exercises/`. Warn the
learner that this wipes their in-progress `problem.py` and clears the
progress entry for that exercise.
