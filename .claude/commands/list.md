---
description: List all exercises with status and tier
argument-hint: "[--tier N]"
---

Run `python marathon.py list $ARGUMENTS` from `exercises/` and present
the result. If the learner passes `--tier N`, only that tier is shown.
