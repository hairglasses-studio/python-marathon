---
description: Silently run all reference solutions against tests
---

Run `python marathon.py verify` from `exercises/`.

Report the pass/fail summary. If any exercises fail, flag them loudly —
this indicates a broken reference solution or a Python compatibility issue.
